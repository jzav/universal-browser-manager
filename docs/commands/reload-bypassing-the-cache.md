# Reload Bypassing the Cache

Keyword: `rec`  
Requires parameters: No  

Reloads the selected tabs while bypassing the browser cache.

When reloading a large number of tabs, many pages may load concurrently, which can temporarily reduce browser responsiveness.
